networks:
  inception:
    driver: bridge

services:
  nginx:
    container_name: nginx
    ports:
      - "443:443"


  wordpress:
    container_name: wordpress

  mariadb:
    container_name: mariadb

services:
  nginx:
  wordpress:
  mariadb:
  redis:
  adminer:
